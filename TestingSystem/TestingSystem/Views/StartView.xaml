<UserControl x:Class="TestingSystem.Views.StartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestingSystem.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Name="StartViewControl">

    <!--<UserControl.CommandBindings>
        <CommandBinding Command="x:SignUpCommand" Executed="SignUp" CanExecute="SignUpCommand_CanExecute"/>
    </UserControl.CommandBindings>-->

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="1.5*"/>
            <RowDefinition/>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="0.3*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="4" Content="Sign Up" Width="100" HorizontalAlignment="Left" />
    </Grid>
</UserControl>
